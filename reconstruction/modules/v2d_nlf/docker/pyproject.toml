[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "v2d-nlf-docker"
version = "0.1.0"
description = "Docker orchestration scripts for NLF"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["v2d.nlf.docker"]

[tool.setuptools.package-dir]
"v2d.nlf.docker" = "."
