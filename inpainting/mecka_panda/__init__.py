"""MECKA-to-Panda pipeline modules.

Kept separate from the shared `inpainting` infrastructure because this pipeline
predates the parallel-jaw/robot renderer contracts and still uses its own
tracking and render schemas.
"""
