motion_file: source/robotic_grounding/robotic_grounding/assets/motion_data/apple_pick_and_place_retarget_motion_w_body.h5

robot:
  type: g1  # Robot type for joint order lookup. Does not impact the robot asset spawned yet.
  qpos_key: qpos
  file_joint_order: mujoco  # "mujoco", "isaaclab", or list of joint names. If "isaaclab", uses the robot's default ordering.

target_object:
  name: apple
  scale: [0.8, 0.8, 0.8]

# fixed_objects: auto-discovered from HDF5 (any *_position keys except target)
