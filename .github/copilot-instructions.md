# Copilot Instructions

Read the repository-wide agent instructions in `AGENTS.md` before making
changes. Keep edits scoped to the active package, avoid heavyweight GPU/Docker
work unless requested, and include the validation commands that match the files
changed.
